# Naive API Client

"Naive" doesn't mean bad, it's just the way you likely to come up with if you try to implement it yourself.
This one also includes handling specific cases for `FormData` and `Blob`, which is something often missed.
